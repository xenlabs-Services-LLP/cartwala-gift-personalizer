(()=>{
  if(window.cartwalaCartFixFast)return;window.cartwalaCartFixFast=true;
  const normalize=text=>String(text||'').replace(/\s+/g,' ').trim();
  const isCdnValue=text=>/cdn\.shopify\.com\/s\/files\//i.test(text||'');
  let lightTimer=0,previewTimer=0,lastDesign='',opening=false;
  const drawerSelector='cart-drawer,#CartDrawer,[data-cart-drawer],.cart-drawer';
  const cartRoot=()=>document.querySelector(drawerSelector)||document;
  const rows=root=>[...root.querySelectorAll('.cart-item,[data-cart-item],cart-drawer-item,.drawer__cart-item')];
  const cleanPrivateRows=root=>{if(!root)return;root.querySelectorAll('dt').forEach(dt=>{if(!/^Your\s+Name\s*:?$/i.test(normalize(dt.textContent)))return;dt.style.setProperty('display','none','important');const dd=dt.nextElementSibling;if(dd?.tagName==='DD')dd.style.setProperty('display','none','important')});root.querySelectorAll('.product-option,.cart-item__property,[class*="property"],li,p,dd,span').forEach(node=>{const text=normalize(node.textContent);if(/^Your\s+Name\s*:\s*.+$/i.test(text)||/^Name\s*:\s*.+$/i.test(text)||isCdnValue(text)||/^_?Personalised\s+Preview\s*:/i.test(text)||/^_?Cartwala\s+/i.test(text))node.style.setProperty('display','none','important')});root.querySelectorAll('a').forEach(link=>{if(!isCdnValue(link.href)&&!isCdnValue(link.textContent))return;const row=link.closest('.product-option,.cart-item__property,li,dd,p');(row||link).style?.setProperty?.('display','none','important')})};
  const getDraft=designId=>new Promise(resolve=>{if(!designId||!('indexedDB'in window)){resolve(null);return}const request=indexedDB.open('cartwala-designs',1);request.onerror=()=>resolve(null);request.onsuccess=()=>{try{const tx=request.result.transaction('drafts','readonly');const get=tx.objectStore('drafts').get(`cart:${designId}`);get.onsuccess=()=>resolve(get.result||null);get.onerror=()=>resolve(null)}catch{resolve(null)}}});
  const setImage=async(image,url,designId)=>{if(!image||!url)return false;image.removeAttribute('srcset');image.removeAttribute('sizes');image.closest('picture')?.querySelectorAll('source').forEach(source=>{source.removeAttribute('srcset');source.removeAttribute('sizes')});image.src=url;image.dataset.cwDraftId=designId||'';image.dataset.cwPreview='true';image.style.objectFit='contain';image.style.background='transparent';await new Promise(resolve=>{if(image.complete&&image.naturalWidth>0){resolve();return}let done=false;const finish=()=>{if(done)return;done=true;resolve()};image.addEventListener('load',finish,{once:true});image.addEventListener('error',finish,{once:true});setTimeout(finish,1500)});return true};
  const applyPreview=async(root,designId,previewUrl)=>{if(!root||!designId)return false;const list=rows(root);if(!list.length)return false;let row=list[list.length-1];try{const response=await fetch((window.Shopify?.routes?.root||'/')+'cart.js',{headers:{Accept:'application/json'},cache:'no-store'});if(response.ok){const cart=await response.json();const index=cart.items.findIndex(item=>item?.properties?.['_Cartwala Design ID']===designId);if(index>=0)row=root.querySelector(`#CartDrawer-Item-${index+1},[data-index="${index+1}"]`)||list[index]||row}}catch{}const image=row?.querySelector('.cart-item__image,img');if(!image)return false;if(previewUrl)try{if(await setImage(image,previewUrl,designId)){lastDesign=designId;return true}}catch{}const record=await getDraft(designId);if(!record?.blob)return false;const url=URL.createObjectURL(record.blob);const ok=await setImage(image,url,designId);if(ok)lastDesign=designId;return ok};
  const restorePreview=async()=>{let designId='';try{designId=sessionStorage.getItem('cartwala-last-design')||''}catch{}if(!designId||designId===lastDesign)return false;return applyPreview(cartRoot(),designId,'')};
  const scheduleLight=(delay=0)=>{clearTimeout(lightTimer);lightTimer=setTimeout(()=>cleanPrivateRows(cartRoot()),delay)};
  const schedulePreview=(delay=60)=>{clearTimeout(previewTimer);previewTimer=setTimeout(restorePreview,delay)};
  const openDrawer=drawer=>{document.body.classList.remove('cw-cart-preparing');if(typeof drawer?.open==='function')drawer.open();else{drawer?.classList.add('active','animate','is-open');drawer?.setAttribute('open','');drawer?.setAttribute('aria-hidden','false');document.body.classList.add('overflow-hidden')}};
  const refreshAndOpenCart=async detail=>{if(opening)return;opening=true;const drawer=document.querySelector(drawerSelector);const designId=detail?.designId||sessionStorage.getItem('cartwala-last-design')||'';const previewUrl=detail?.previewUrl||'';try{if(!drawer)throw new Error('Cart drawer not found');const root=window.Shopify?.routes?.root||'/';const response=await fetch(`${root}?sections=cart-drawer,cart-icon-bubble&_cw=${Date.now()}`,{credentials:'same-origin',cache:'no-store',headers:{Accept:'application/json'}});if(!response.ok)throw new Error('Cart drawer refresh failed');const sections=await response.json();const html=sections?.['cart-drawer'];if(html){const doc=new DOMParser().parseFromString(html,'text/html');const fresh=doc.querySelector(drawerSelector);if(fresh){drawer.innerHTML=fresh.innerHTML;drawer.className=fresh.className;drawer.classList.remove('is-empty')}}const bubbleHtml=sections?.['cart-icon-bubble'];if(bubbleHtml){const doc=new DOMParser().parseFromString(bubbleHtml,'text/html');const fresh=doc.querySelector('#cart-icon-bubble,.cart-count-bubble,[data-cart-count]');const current=document.querySelector('#cart-icon-bubble,.cart-count-bubble,[data-cart-count]');if(fresh&&current)current.replaceWith(fresh)}lastDesign='';cleanPrivateRows(drawer);if(designId)await applyPreview(drawer,designId,previewUrl);cleanPrivateRows(drawer);openDrawer(drawer);document.dispatchEvent(new CustomEvent('cartwala:drawer-ready',{bubbles:true,detail:{designId}}))}catch(error){console.warn('Cartwala drawer sync failed',error);document.body.classList.remove('cw-cart-preparing');document.dispatchEvent(new CustomEvent('cartwala:drawer-error',{bubbles:true}))}finally{opening=false}};
  const observer=new MutationObserver(()=>scheduleLight(0));observer.observe(document.documentElement,{childList:true,subtree:true});
  document.addEventListener('cartwala:item-added',event=>refreshAndOpenCart(event.detail||{}));
  document.addEventListener('DOMContentLoaded',()=>{scheduleLight(0);schedulePreview(50)},{once:true});window.addEventListener('pageshow',()=>{scheduleLight(0);schedulePreview(50)});scheduleLight(0);schedulePreview(50);
})();
